apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: front
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer  # 외부에서 접근 가능하게 설정
